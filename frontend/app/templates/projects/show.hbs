<h3>{{name}}</h3>

<h5>Orchestrated by {{link-to user.fullName 'users.show' user }}</h5>

<div id="description">
  {{description}}
</div>

<h4>Tasks</h4>

<ul>
  {{#each task in tasks}}
    <li>{{task.name}}</li>
  {{/each}}
</ul>
