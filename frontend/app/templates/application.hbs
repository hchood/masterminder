<h2 id="title">Mastermind</h2>
<ul>
  <li>{{link-to 'Projects' 'projects.index'}}</li>
  <li>{{link-to 'Add a project' 'projects.new'}}</li>

  {{#if session.isAuthenticated}}
    <li><a href="#" {{ action 'invalidateSession' }}>Logout</a></li>
  {{else}}
    <li>{{link-to 'Login' 'login'}}</li>
  {{/if}}
</ul>

{{outlet}}
