{{task.name}}

{{#if task.completedAt}}
  Completed
{{else}}
  {{#if session.isAuthenticated}}
    {{#if task.project.canEdit}}
      <button {{action 'saveTask'}}>Complete</button>
    {{/if}}
  {{/if}}
{{/if}}
