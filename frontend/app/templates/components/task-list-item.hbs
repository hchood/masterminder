{{#if task.isCompleted}}
  {{input type="checkbox"
    checked=task.isCompleted
    class="toggle"}}
{{else}}
  {{#if task.project.canEdit}}
    {{input type="checkbox"
      checked=task.isCompleted
      class="toggle"}}
  {{/if}}
{{/if}}

{{task.name}}
