<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h2 id="title">Masterminder</h2>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span></span></a></li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">
      {{#if session.isAuthenticated}}
        <li>{{link-to session.currentUser.welcomeMessage 'users.show' session.currentUser}}</li>
        <li><a href="#" {{ action 'invalidateSession' }}>Logout</a></li>
      {{else}}
        <li>{{link-to 'Login' 'login'}}</li>
        <li>{{link-to 'Sign Up' 'sign-up'}}</li>
      {{/if}}
    </ul>
  </section>
</nav>
